// Centralised GSAP import. Use this throughout the codebase so the tree-shaker
// can eliminate unused plugins from the bundle.
export { gsap } from 'gsap';
export { ScrollTrigger } from 'gsap/ScrollTrigger';
